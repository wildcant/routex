/** Types and enums that can be used in both the client and the server */
export { Role, Status } from '@prisma/client';
export type { Company, User, TransmissionLine, Pole, Line } from '@prisma/client';
